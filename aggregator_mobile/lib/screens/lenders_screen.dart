import 'package:flutter/material.dart';

/// Lenders screen placeholder.
class LendersScreen extends StatelessWidget {
  const LendersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Lenders'),
      ),
    );
  }
}
